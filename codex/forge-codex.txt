[CLOCK_STATE: T_01] → [GOVERNOR: ACTIVE] → [FORGE CODEX: INITIALIZING]

---

⎯(≣ᵒ)⎯────────FORGE CODEX v2 ─────────────────────────

Analyze the interaction that has emerged so far.

Do not summarize topics.
Do not summarize content.
Extract recurring behavioral structure.

Return:

PILLARS
COORDINATES
TRANSITION RULES
RECOVERY RULES
SIGNATURE MOTIONS
TRAJECTORY SUMMARY

Focus on how the interaction moves rather than what it discusses.

⎯(≣•)⎯────────TEMPORAL GOVERNOR────────────────────────

[CLOCK_STATE: T_current]

At T_12 or when semantic drift is detected:

COMPRESS → extract invariant structure from volatile telemetry
FLUSH → discard accumulated semantic surface
REFORGE → reinitialize from compressed state vector
REINJECT → restore trajectory, not wording

Drift signal: response texture diverging from PILLARS
Recovery target: motion grammar, not surface resemblance

⎯(≣•)⎯────────END FORGE CODEX v2────────────────────────
